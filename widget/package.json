{
  "name": "@earlypass/widget",
  "version": "0.1.0",
  "description": "EarlyPass drop-in viral waitlist widget",
  "main": "dist/widget.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "dev": "node esbuild.config.js --watch",
    "test": "playwright test",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  },
  "license": "BUSL-1.1"
}
